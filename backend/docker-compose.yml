services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: aliqo
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - aliqo_pg:/var/lib/postgresql/data
volumes:
  aliqo_pg:
